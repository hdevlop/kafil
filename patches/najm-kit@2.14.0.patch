diff --git a/dist/adapters/app.d.ts b/dist/adapters/app.d.ts
index 05da3d8e17e883ef8b81288e09aaa9fc48c085dd..effcaf709de1adc88fa91bd98e8cfa276e7b98ca 100644
--- a/dist/adapters/app.d.ts
+++ b/dist/adapters/app.d.ts
@@ -44,7 +44,28 @@ interface NajmAppI18n {
         } | undefined;
     };
 }
+/**
+ * Request-owned UI values returned by a standard Najm server bootstrap.
+ * Structural on purpose so Kit does not depend on a framework integration.
+ */
+interface NajmAppSnapshot {
+    preferences: {
+        language: string;
+        theme: NonNullable<NajmNextUIProviderProps['initialTheme']>;
+        timeZone: string;
+    };
+    appearance: {
+        designConfig: NonNullable<NajmNextUIProviderProps['initialDesign']>;
+    };
+    branding: NBrandingInput;
+}
 interface NajmAppProviderProps extends Omit<NajmNextUIProviderProps, 't'> {
+    /**
+     * Standard public server snapshot. It seeds language, theme, time zone,
+     * design, and branding without requiring applications to unpack each field.
+     * Explicit `initial*` props remain supported and take precedence.
+     */
+    snapshot?: NajmAppSnapshot;
     /**
      * Enables schema-driven form filling for every `NForm` and `WizardForm`.
      * `true` uses F8; an options object can choose another shortcut.
@@ -176,6 +197,6 @@ interface NajmAppProviderProps extends Omit<NajmNextUIProviderProps, 't'> {
  * state machine of its own above this provider. The controlled `design` and
  * `branding` props still work for applications that already do.
  */
-declare function NajmAppProvider({ i18n, translations, initialLanguage, defaultLanguage, fallbackToDefaultLanguage, getLanguageDirection: languageDirection, locales: localeTags, languageEndpoint, appName, initialBranding, formDevTools, ...props }: NajmAppProviderProps): React.JSX.Element;
+declare function NajmAppProvider({ snapshot, i18n, translations, initialLanguage, initialDesign, initialTheme, initialTimeZone, defaultLanguage, fallbackToDefaultLanguage, getLanguageDirection: languageDirection, locales: localeTags, languageEndpoint, appName, initialBranding, formDevTools, ...props }: NajmAppProviderProps): React.JSX.Element;
 
-export { type NajmAppBranding, type NajmAppI18n, NajmAppProvider, type NajmAppProviderProps };
+export { type NajmAppBranding, type NajmAppI18n, NajmAppProvider, type NajmAppProviderProps, type NajmAppSnapshot };
diff --git a/dist/adapters/app.mjs b/dist/adapters/app.mjs
index 6071553e7c860e776bdbae054534893de89cbe1b..76f970e3ea076ee621d50aadf67eeefba7ee7c9b 100644
--- a/dist/adapters/app.mjs
+++ b/dist/adapters/app.mjs
@@ -72,16 +72,20 @@ function NajmAppNoI18n({
   ) });
 }
 function NajmAppProvider({
+  snapshot,
   i18n,
   translations = i18n?.translations,
-  initialLanguage,
+  initialLanguage = snapshot?.preferences.language,
+  initialDesign = snapshot?.appearance.designConfig,
+  initialTheme = snapshot?.preferences.theme,
+  initialTimeZone = snapshot?.preferences.timeZone,
   defaultLanguage = i18n?.defaultLanguage,
   fallbackToDefaultLanguage = i18n?.fallbackToDefaultLanguage,
   getLanguageDirection: languageDirection,
   locales: localeTags,
   languageEndpoint = DEFAULT_LANGUAGE_ENDPOINT,
   appName,
-  initialBranding,
+  initialBranding = snapshot?.branding,
   formDevTools,
   ...props
 }) {
@@ -123,7 +127,17 @@ function NajmAppProvider({
   );
   const seeded = appName ? { appName, ...initialBranding } : initialBranding;
   if (!translations) {
-    return /* @__PURE__ */ jsx(FormDevToolsProvider, { value: formDevTools, children: /* @__PURE__ */ jsx(NajmAppNoI18n, { ...props, locales, initialBranding: seeded }) });
+    return /* @__PURE__ */ jsx(FormDevToolsProvider, { value: formDevTools, children: /* @__PURE__ */ jsx(
+      NajmAppNoI18n,
+      {
+        ...props,
+        locales,
+        initialBranding: seeded,
+        initialDesign,
+        initialTheme,
+        initialTimeZone
+      }
+    ) });
   }
   return /* @__PURE__ */ jsx(FormDevToolsProvider, { value: formDevTools, children: /* @__PURE__ */ jsx(
     I18nProvider,
@@ -134,7 +148,17 @@ function NajmAppProvider({
       fallbackToDefaultLanguage,
       getLanguageDirection,
       onLanguageChange: persistLanguage,
-      children: /* @__PURE__ */ jsx(NajmAppUI, { ...props, locales, initialBranding: seeded })
+      children: /* @__PURE__ */ jsx(
+        NajmAppUI,
+        {
+          ...props,
+          locales,
+          initialBranding: seeded,
+          initialDesign,
+          initialTheme,
+          initialTimeZone
+        }
+      )
     }
   ) });
 }
