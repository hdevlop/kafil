# Sponsor overview visual evidence

The managed Playwright closeout workflow generates the screenshots in this
directory:

```bash
bun run --cwd apps/web test:e2e
```

The evidence covers the populated operator overview at desktop and mobile
widths, the inactive and zero-data state, dark theme, Arabic RTL, and the
populated sponsor self-dashboard.
