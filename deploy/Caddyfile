{
	email {$ACME_EMAIL:ops@example.invalid}
	admin off
}

{$KAFIL_HOSTNAME:demo.example.invalid} {
	encode zstd gzip

	header {
		-Server
		-X-Powered-By
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		# The Next.js proxy owns CSP because its script nonce is unique per request.
		# An edge CSP here would overwrite that nonce and block framework scripts.
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		Referrer-Policy "strict-origin-when-cross-origin"
		Permissions-Policy "camera=(), geolocation=(), microphone=()"
	}

	reverse_proxy app:3000
}
