/** Single source for the product name: page metadata, logo alt text, PWA title. */
export const APP_NAME = "Kafil";

/**
 * Everything else that used to live here — slot keys, admin config shapes,
 * draft shapes, upload inputs, byte ceilings, accept strings, the asset route
 * prefix — is `najm-theme`'s contract. Import `PublicBranding`,
 * `AdminBranding`, and `STANDARD_BRANDING_SLOTS` from `najm-theme` rather than
 * restating them here.
 */
